.container
	.row
		h1.text-center.login-title Sign in to our Passport app
			.account-wall
				form.form-signin(action='/login', method='POST')
					input.input-group.input-group-lg.form-control(type='text', name='username', placeholder='Email',required, autofocus)
					input.form-control(type='password', name='password', placeholder='Password', required)
					button.btn.btn-lg.btn-primary.btn-block(type='submit') Sign in
					span.clearfix
			a.text-center.new-account(href='/signup') Create an account
			#message
			if message
				h1.text-center.error-message #{message}
